from shellmancer.game.clock import clock

class DummyData:
    default_msgs = dict(
        win_condition=("You successfully completed the objective", 0),
        fail_condition=("You fucked up the mission", 0)
    )

